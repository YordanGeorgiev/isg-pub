{
	# the name of this web application layer
	 'web_app' => 'doc_pub' 

	# the port number of the db 
	, 'web_port' => "5001"

	# the name of the default db 
	, 'database' => 'isg_pub_en' 

	# the host name of the db 
	, 'db_host' => "localhost"

	# the port number of the db 
	, 'db_port' => "13306"

	# the db user 
	, 'db_user' => "usrisgpuben"

	# the db pw
	, 'db_user_pw' => "secret"

	# the host name of the web host
	, 'web_host' => 'localhost'
};
