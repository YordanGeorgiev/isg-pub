Once the user has the mutt binary installed and configured .. the script will send the latest
created package with the create-full-package and create-relative-package commands to the configured
in the configuration files Emails ( separated by space ) 
by using the followig calls: 

bash sfw/bash/isg-pub/isg-pub.sh -a create-full-package -a gmail-package

